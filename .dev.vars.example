# Local secrets for `wrangler dev` - copy to .dev.vars (git-ignored)
# In production set them with: npx wrangler secret put EMAIL_USER / EMAIL_PASS
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password
